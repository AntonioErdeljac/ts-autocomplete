import React from 'react';
import { render } from '@testing-library/react';

import CloseIcon from './index';

describe('CloseIcon', () => {
  it('Renders', () => {
    render(<CloseIcon />);
  });
});
